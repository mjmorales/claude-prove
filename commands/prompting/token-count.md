---
description: Estimate token counts for prompt files using glob patterns or file paths.
argument-hint: "[glob pattern or file path] [--sort tokens|name|lines] [--json] [--no-strip]"
---

# Token Count: $ARGUMENTS

Load and follow the prompting-token-count skill (`skills/prompting-token-count/SKILL.md` from the workflow plugin).
