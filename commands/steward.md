---
description: Deep codebase quality audit with automated fixes. Runs the code-steward agent for line-by-line review, produces a findings document, then orchestrates parallel subagents to implement all fixes.
argument-hint: "[directory or module to scope the audit]"
---

# Code Steward: Deep Audit

Run a comprehensive source code audit with automated fixes.

Load and follow the steward skill (`skills/steward/SKILL.md` from the workflow plugin).
