---
description: Code quality audit — session review (default), full deep audit, or iterative auto-fix loop. Selects mode via flags.
argument-hint: "[--review|--full|--auto] [--max-passes N] [scope]"
---

# Code Steward: $ARGUMENTS

Load and follow the steward skill (`skills/steward/SKILL.md` from the workflow plugin). Pass `$ARGUMENTS` through for mode dispatch.
