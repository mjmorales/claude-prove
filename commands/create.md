---
description: Create a new Claude Code skill, slash command, subagent, or technical spec.
argument-hint: "<skill|command|agent|spec> [name]"
---

# Create: $ARGUMENTS

Load and follow the create skill (`skills/create/SKILL.md` from the workflow plugin). Pass `$ARGUMENTS` for type dispatch and subject.
