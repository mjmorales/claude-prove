---
description: "Run the orchestrator end-to-end: requirements -> plan -> parallel worktree execution -> merge"
---

# Full Auto: $ARGUMENTS

Load and follow the orchestrator skill (`skills/orchestrator/SKILL.md`).

**Mode**: Full-auto — no plan exists yet. Start with "Full Mode: Requirements Gathering (PRD)", then proceed through all phases: Initialization, Plan Review, Execution Loop, Completion, Merge & Cleanup.
