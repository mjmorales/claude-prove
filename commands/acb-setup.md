---
description: Set up, diagnose, or document ACB intent hooks — install, migrate, and provide agent reference
argument-hint: "[setup | doctor | info]"
---

# ACB Setup: $ARGUMENTS

Load and follow the acb-setup skill (`skills/acb-setup/SKILL.md` from the workflow plugin). Default subcommand is `setup` if no arguments provided.
