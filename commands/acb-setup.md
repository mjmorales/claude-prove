---
name: acb-setup
description: Set up, diagnose, or document ACB intent hooks — install, migrate, and provide agent reference
argument-hint: "[setup | doctor | info]"
allowed_tools: Bash, Read, Write, Glob, Grep, AskUserQuestion
---

# ACB Setup

Load and follow the acb-setup skill (`skills/acb-setup/SKILL.md`).

Pass `$ARGUMENTS` through to the skill. Default subcommand is `setup` if no arguments provided.
