---
description: Create a handoff prompt (.prove/handoff.md) for clean session-to-session context transfer
argument-hint: "[brief note about what to hand off]"
---

# Handoff: $ARGUMENTS

Load and follow the handoff skill (`skills/handoff/SKILL.md`).

Pass `$ARGUMENTS` as additional user-supplied context for the pickup note.
