---
description: Configure orchestrator notification reporters (Slack, Discord, MCP, custom)
argument-hint: "[platform]"
---

# Notify Setup

Load and follow the notify-setup skill (`skills/notify-setup/SKILL.md`).

If `$ARGUMENTS` specifies a platform, skip platform selection and use it directly.
