---
description: Execute a whole milestone or plan.json task tree as a parallel fan-out workflow
argument-hint: "<milestone-id | plan.json> [--backend auto|dynamic|native] [--max-agents n] [--verify tag] [--dry-run]"
core: true
summary: Run a milestone/task tree as parallel waves via orchestrator full-mode, mirroring status to scrum
---

# Workflow: $ARGUMENTS

Load and follow the workflow skill (`skills/workflow/SKILL.md`), passing `$ARGUMENTS` to its **Input Resolution** section.
