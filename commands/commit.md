# Semantic Commit Assistant

Analyze all staged and unstaged changes, group them into logical units, and create semantic commits.

Load and follow the commit skill (`skills/commit/SKILL.md` from the workflow plugin).
