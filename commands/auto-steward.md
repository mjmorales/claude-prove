---
description: Iterative code quality audit that loops until clean. Runs steward in a bounded fix-audit cycle with human approval on first pass only.
argument-hint: "[--full] [--max-passes N] [directory or module]"
---

# Auto Steward: Iterative Audit Loop

Run an iterative code quality audit that keeps fixing and re-auditing until clean.

Load and follow the auto-steward skill (`skills/auto-steward/SKILL.md` from the workflow plugin).
