{
  "name": "review-ui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": ["server", "web"],
  "scripts": {
    "dev": "concurrently -k -n server,web -c cyan,magenta \"npm -w server run dev\" \"npm -w web run dev\"",
    "build": "npm -w server run build && npm -w web run build",
    "start": "npm -w server run start"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
