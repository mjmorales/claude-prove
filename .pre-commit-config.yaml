repos:
  - repo: local
    hooks:
      - id: conventional-commit-scope
        name: Validate conventional commit with .prove.json scopes
        entry: bun run /Users/manuelmorales/dev/claude-prove/packages/cli/bin/run.ts commit validate-msg
        language: system
        stages: [commit-msg]
        always_run: true
