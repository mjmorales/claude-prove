{
  "name": "claude-prove",
  "private": true,
  "version": "0.36.0",
  "type": "module",
  "workspaces": ["packages/*", "packages/review-ui/server", "packages/review-ui/web"],
  "scripts": {
    "test": "bun test",
    "lint": "biome check .",
    "format": "biome format --write .",
    "typecheck": "tsc --build",
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/bun": "^1.2.22",
    "typescript": "^5.6.3"
  },
  "packageManager": "bun@1.2.22",
  "engines": {
    "bun": ">=1.2.0"
  }
}
