{
  "name": "@acb/core",
  "version": "0.1.0",
  "description": "Core library for Agent Change Brief (ACB) documents — parsing, validation, and review state management",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "acb-review": "dist/cli/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^2.0.0"
  },
  "files": [
    "dist"
  ],
  "license": "MIT"
}
