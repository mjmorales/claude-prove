import { StubCommand } from '../../stub-command';

export default class Acb extends StubCommand {
  static override description = 'Agent change brief review (not yet implemented)';
  protected readonly phase = 9;
}
