import { StubCommand } from '../../stub-command';

export default class Scrum extends StubCommand {
  static override description = 'Agentic task management (not yet implemented)';
  protected readonly phase = 12;
}
