index	idx_acb_acb_documents_branch	CREATE INDEX idx_acb_acb_documents_branch ON acb_acb_documents (branch)
index	idx_acb_group_verdicts_key	CREATE INDEX idx_acb_group_verdicts_key ON acb_group_verdicts (slug, group_id)
index	idx_acb_group_verdicts_slug	CREATE INDEX idx_acb_group_verdicts_slug ON acb_group_verdicts (slug)
index	idx_acb_manifests_branch	CREATE INDEX idx_acb_manifests_branch ON acb_manifests (branch)
index	idx_acb_manifests_branch_sha	CREATE INDEX idx_acb_manifests_branch_sha ON acb_manifests (branch, commit_sha)
index	idx_acb_manifests_run_slug	CREATE INDEX idx_acb_manifests_run_slug ON acb_manifests (run_slug, branch)
index	idx_acb_review_state_branch	CREATE INDEX idx_acb_review_state_branch ON acb_review_state (branch)
index	idx_scrum_acceptance_criteria_task	CREATE INDEX idx_scrum_acceptance_criteria_task ON scrum_acceptance_criteria (task_id)
index	idx_scrum_annotations_target	CREATE INDEX idx_scrum_annotations_target ON scrum_annotations (target_kind, target_ref)
index	idx_scrum_asks_blocking_artifact	CREATE INDEX idx_scrum_asks_blocking_artifact ON scrum_asks (blocking_artifact)
index	idx_scrum_asks_to_team	CREATE INDEX idx_scrum_asks_to_team ON scrum_asks (to_team)
index	idx_scrum_contributors_email	CREATE INDEX idx_scrum_contributors_email ON scrum_contributors (email)
index	idx_scrum_contributors_github	CREATE INDEX idx_scrum_contributors_github ON scrum_contributors (github)
index	idx_scrum_criterion_verdicts_criterion	CREATE INDEX idx_scrum_criterion_verdicts_criterion ON scrum_criterion_verdicts (criterion_id)
index	idx_scrum_decisions_status	CREATE INDEX idx_scrum_decisions_status ON scrum_decisions (status)
index	idx_scrum_decisions_topic	CREATE INDEX idx_scrum_decisions_topic ON scrum_decisions (topic)
index	idx_scrum_deps_to_task	CREATE INDEX idx_scrum_deps_to_task ON scrum_deps (to_task_id)
index	idx_scrum_escalations_task_state	CREATE INDEX idx_scrum_escalations_task_state ON scrum_escalations (task_id, state)
index	idx_scrum_escalations_walked_up_from	CREATE INDEX idx_scrum_escalations_walked_up_from ON scrum_escalations (walked_up_from)
index	idx_scrum_events_task_ts	CREATE INDEX idx_scrum_events_task_ts ON scrum_events (task_id, ts DESC)
index	idx_scrum_lores_team	CREATE INDEX idx_scrum_lores_team ON scrum_lores (team_slug)
index	idx_scrum_operator_history_interval	CREATE INDEX idx_scrum_operator_history_interval ON scrum_operator_history (from_ts, to_ts)
index	idx_scrum_run_links_path	CREATE INDEX idx_scrum_run_links_path ON scrum_run_links (run_path)
index	idx_scrum_tags_tag	CREATE INDEX idx_scrum_tags_tag ON scrum_tags (tag)
index	idx_scrum_tasks_parent	CREATE INDEX idx_scrum_tasks_parent ON scrum_tasks (parent_id)
index	idx_scrum_tasks_status_event	CREATE INDEX idx_scrum_tasks_status_event ON scrum_tasks (status, last_event_at DESC)
index	idx_scrum_team_accepts_team	CREATE INDEX idx_scrum_team_accepts_team ON scrum_team_accepts (team_slug)
index	idx_scrum_team_exposes_team	CREATE INDEX idx_scrum_team_exposes_team ON scrum_team_exposes (team_slug)
index	idx_scrum_team_members_team_role	CREATE INDEX idx_scrum_team_members_team_role ON scrum_team_members (team_slug, role)
index	idx_scrum_team_scopes_team	CREATE INDEX idx_scrum_team_scopes_team ON scrum_team_scopes (team_slug)
index	idx_scrum_teams_type	CREATE INDEX idx_scrum_teams_type ON scrum_teams (team_type)
table	_migrations_log	CREATE TABLE _migrations_log (domain TEXT NOT NULL, version INTEGER NOT NULL, description TEXT NOT NULL, applied_at TEXT NOT NULL, PRIMARY KEY (domain, version))
table	acb_acb_documents	CREATE TABLE acb_acb_documents (id TEXT PRIMARY KEY, branch TEXT NOT NULL, data TEXT NOT NULL, created_at TEXT NOT NULL)
table	acb_group_verdicts	CREATE TABLE acb_group_verdicts (id TEXT PRIMARY KEY, slug TEXT NOT NULL, group_id TEXT NOT NULL, verdict TEXT NOT NULL, note TEXT, fix_prompt TEXT, created_at TEXT NOT NULL)
table	acb_manifests	CREATE TABLE acb_manifests (id TEXT PRIMARY KEY, branch TEXT NOT NULL, commit_sha TEXT NOT NULL, timestamp TEXT NOT NULL, data TEXT NOT NULL, created_at TEXT NOT NULL, run_slug TEXT)
table	acb_review_state	CREATE TABLE acb_review_state (id TEXT PRIMARY KEY, branch TEXT NOT NULL, acb_hash TEXT NOT NULL, data TEXT NOT NULL, created_at TEXT NOT NULL)
table	scrum_acceptance_criteria	CREATE TABLE scrum_acceptance_criteria (id TEXT PRIMARY KEY, task_id TEXT NOT NULL REFERENCES scrum_tasks (id), criterion_id TEXT NOT NULL, ord TEXT NOT NULL, text TEXT NOT NULL, verifies_by TEXT NOT NULL CHECK (verifies_by IN ('bash', 'assert', 'gate', 'agent')), check_payload TEXT NOT NULL, status TEXT NOT NULL DEFAULT 'active' CHECK (status IN ('active', 'superseded')), idempotent INTEGER NOT NULL DEFAULT 0, scope TEXT, timeout TEXT, superseded_by TEXT, reason TEXT, inherited_from TEXT, created_at TEXT NOT NULL, UNIQUE (task_id, criterion_id))
table	scrum_annotations	CREATE TABLE scrum_annotations (id TEXT PRIMARY KEY, target_kind TEXT NOT NULL, target_ref TEXT NOT NULL, body TEXT NOT NULL, author TEXT NOT NULL, created_at TEXT NOT NULL)
table	scrum_asks	CREATE TABLE scrum_asks (id TEXT PRIMARY KEY, from_team TEXT NOT NULL REFERENCES scrum_teams (slug), to_team TEXT NOT NULL REFERENCES scrum_teams (slug), ask_type TEXT NOT NULL, blocking_artifact TEXT NOT NULL REFERENCES scrum_tasks (id), state TEXT NOT NULL DEFAULT 'filed', created_at TEXT NOT NULL, mapped_artifact TEXT, rejected_reason TEXT, counter_proposal TEXT)
table	scrum_context_bundles	CREATE TABLE scrum_context_bundles (task_id TEXT PRIMARY KEY REFERENCES scrum_tasks (id), rebuilt_at TEXT NOT NULL, bundle_json TEXT NOT NULL)
table	scrum_contributors	CREATE TABLE scrum_contributors (id TEXT PRIMARY KEY, slug TEXT NOT NULL UNIQUE, status TEXT NOT NULL DEFAULT 'active', display_name TEXT, github TEXT, email TEXT, created_by TEXT, created_at TEXT NOT NULL, last_modified_by TEXT, last_modified_at TEXT)
table	scrum_criterion_verdicts	CREATE TABLE scrum_criterion_verdicts (id TEXT PRIMARY KEY, criterion_id TEXT NOT NULL REFERENCES scrum_acceptance_criteria (id), channel TEXT NOT NULL CHECK (channel IN ('gate', 'verification')), verdict TEXT NOT NULL, reason TEXT, by_whom TEXT, comment TEXT, at TEXT NOT NULL)
table	scrum_decisions	CREATE TABLE scrum_decisions (id TEXT PRIMARY KEY, title TEXT NOT NULL, topic TEXT, status TEXT NOT NULL DEFAULT 'accepted', content TEXT NOT NULL, source_path TEXT, content_sha TEXT NOT NULL, recorded_at TEXT NOT NULL, recorded_by_agent TEXT, superseded_by TEXT REFERENCES scrum_decisions (id), reason TEXT, kind TEXT, write_status TEXT, gate_responder TEXT, gate_responded_at TEXT, source_lore_id TEXT REFERENCES scrum_lores (id))
table	scrum_deps	CREATE TABLE scrum_deps (from_task_id TEXT NOT NULL REFERENCES scrum_tasks (id), to_task_id TEXT NOT NULL REFERENCES scrum_tasks (id), kind TEXT NOT NULL CHECK (kind IN ('blocks', 'blocked_by')), PRIMARY KEY (from_task_id, to_task_id, kind))
table	scrum_escalations	CREATE TABLE scrum_escalations (id TEXT PRIMARY KEY, task_id TEXT NOT NULL, escalation_type TEXT NOT NULL, layer TEXT NOT NULL, state TEXT NOT NULL DEFAULT 'open', summary TEXT NOT NULL, raised_by TEXT, resolution_mode TEXT, resolution_note TEXT, resolved_by TEXT, walked_up_from TEXT REFERENCES scrum_escalations (id), attributes TEXT, created_at TEXT NOT NULL, resolved_at TEXT)
table	scrum_events	CREATE TABLE scrum_events (id TEXT PRIMARY KEY, task_id TEXT NOT NULL REFERENCES scrum_tasks (id), ts TEXT NOT NULL, kind TEXT NOT NULL, agent TEXT, payload_json TEXT NOT NULL)
table	scrum_lores	CREATE TABLE scrum_lores (id TEXT PRIMARY KEY, team_slug TEXT NOT NULL REFERENCES scrum_teams (slug), body TEXT NOT NULL, author_contributor_id TEXT NOT NULL, created_at TEXT NOT NULL, superseded_by TEXT, reason TEXT)
table	scrum_milestones	CREATE TABLE scrum_milestones (id TEXT PRIMARY KEY, title TEXT NOT NULL, description TEXT, target_state TEXT, status TEXT NOT NULL, created_at TEXT NOT NULL, closed_at TEXT, initiative TEXT)
table	scrum_operator_history	CREATE TABLE scrum_operator_history (id TEXT PRIMARY KEY, contributor_id TEXT NOT NULL REFERENCES scrum_contributors (id), from_ts TEXT NOT NULL, to_ts TEXT, created_at TEXT NOT NULL, created_by TEXT)
table	scrum_run_links	CREATE TABLE scrum_run_links (task_id TEXT NOT NULL REFERENCES scrum_tasks (id), run_path TEXT NOT NULL, branch TEXT, slug TEXT, linked_at TEXT NOT NULL, PRIMARY KEY (task_id, run_path))
table	scrum_tags	CREATE TABLE scrum_tags (task_id TEXT NOT NULL REFERENCES scrum_tasks (id), tag TEXT NOT NULL, added_at TEXT NOT NULL, PRIMARY KEY (task_id, tag))
table	scrum_tasks	CREATE TABLE scrum_tasks (id TEXT PRIMARY KEY, title TEXT NOT NULL, description TEXT, status TEXT NOT NULL, milestone_id TEXT REFERENCES scrum_milestones (id), created_by_agent TEXT, created_at TEXT NOT NULL, last_event_at TEXT, deleted_at TEXT, parent_id TEXT REFERENCES scrum_tasks (id), layer TEXT, acceptance_policy_json TEXT, bounds_json TEXT, terminal_reason TEXT, terminal_detail TEXT, last_modified_by TEXT, last_modified_at TEXT, worker_id TEXT, run_id TEXT, team_slug TEXT)
table	scrum_team_accepts	CREATE TABLE scrum_team_accepts (id TEXT PRIMARY KEY, team_slug TEXT NOT NULL REFERENCES scrum_teams (slug), ask_type TEXT NOT NULL, status TEXT NOT NULL DEFAULT 'active', superseded_by TEXT REFERENCES scrum_team_accepts (id), reason TEXT, created_at TEXT NOT NULL)
table	scrum_team_exposes	CREATE TABLE scrum_team_exposes (id TEXT PRIMARY KEY, team_slug TEXT NOT NULL REFERENCES scrum_teams (slug), name TEXT NOT NULL, schema_ref TEXT NOT NULL, status TEXT NOT NULL DEFAULT 'active', superseded_by TEXT REFERENCES scrum_team_exposes (id), reason TEXT, created_at TEXT NOT NULL)
table	scrum_team_members	CREATE TABLE scrum_team_members (id TEXT PRIMARY KEY, team_slug TEXT NOT NULL REFERENCES scrum_teams (slug), role TEXT NOT NULL, contributor_id TEXT NOT NULL, from_ts TEXT NOT NULL, to_ts TEXT, reason TEXT, created_at TEXT NOT NULL)
table	scrum_team_scopes	CREATE TABLE scrum_team_scopes (team_slug TEXT NOT NULL REFERENCES scrum_teams (slug), kind TEXT NOT NULL, "glob" TEXT NOT NULL, PRIMARY KEY (team_slug, kind, "glob"))
table	scrum_teams	CREATE TABLE scrum_teams (slug TEXT PRIMARY KEY, team_type TEXT NOT NULL, charter TEXT, lifetime TEXT NOT NULL DEFAULT 'persistent', created_at TEXT NOT NULL, terminates_on_milestone TEXT, status TEXT NOT NULL DEFAULT 'active')
view	acb_acb_documents_head	CREATE VIEW acb_acb_documents_head AS SELECT h.branch, h.data, (SELECT MIN (d.created_at) FROM acb_acb_documents d WHERE d.branch = h.branch) AS created_at, h.created_at AS updated_at FROM acb_acb_documents h WHERE h.id = (SELECT MAX (d2.id) FROM acb_acb_documents d2 WHERE d2.branch = h.branch)
view	acb_group_verdicts_head	CREATE VIEW acb_group_verdicts_head AS SELECT h.slug, h.group_id, h.verdict, h.note, h.fix_prompt, h.created_at AS updated_at FROM acb_group_verdicts h WHERE h.id = (SELECT MAX (v2.id) FROM acb_group_verdicts v2 WHERE v2.slug = h.slug AND v2.group_id = h.group_id)
view	acb_review_state_head	CREATE VIEW acb_review_state_head AS SELECT h.branch, h.acb_hash, h.data, (SELECT MIN (r.created_at) FROM acb_review_state r WHERE r.branch = h.branch) AS created_at, h.created_at AS updated_at FROM acb_review_state h WHERE h.id = (SELECT MAX (r2.id) FROM acb_review_state r2 WHERE r2.branch = h.branch)
view	scrum_criterion_head	CREATE VIEW scrum_criterion_head AS SELECT v.criterion_id, v.channel, v.verdict, v.reason, v.by_whom, v.comment, v.at FROM scrum_criterion_verdicts v WHERE v.id = (SELECT MAX (v2.id) FROM scrum_criterion_verdicts v2 WHERE v2.criterion_id = v.criterion_id)
