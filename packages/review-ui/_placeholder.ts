// Placeholder to satisfy TypeScript's composite project requirement (needs >=1 input).
// Phase 11 absorbed the review UI into this workspace; server/ and web/ keep
// their own non-composite tsconfigs and are built via their own npm scripts
// (see README.md). Later waves consolidate the build.
export {};
